const benar-usr = document.querySelector('.container .username');
const benar-pasa = document.querySelector('.container .password');

if(benar-usr == test){
	result.style.color = 'green';
	result.innerHTML = "Username Benar"
}